# Create this API key at https://create.roblox.com/dashboard/credentials
# (or your experience's Open Cloud settings). Scope it to only the specific
# universe(s)/data store(s)/MessagingService topics you actually need.
ROBLOX_OPEN_CLOUD_API_KEY=
